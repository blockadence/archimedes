#!/usr/bin/env bash
# Should never be invoked -- run-driver.sh must reject the missing
# fixed_path before running any driver command.
echo "stub-fixed-no-path should not have been invoked" >&2
exit 1
