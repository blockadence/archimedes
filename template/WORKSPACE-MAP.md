# Workspace Map

## Repos

(populated by `scripts/bootstrap.sh`, then `scripts/render-map.sh`)

## Relationships

(hand-written; describe cross-repo dependencies here as you learn them, e.g.:
`service-a → service-b`: service-b vendors service-a's client library, so a
release of service-a needs a follow-up dependency-bump PR in service-b.)
