# driver: <name>   # optional instance-wide default driver (drivers/<name>),
# used by `archimedes context-map` when a repo below sets no driver of its
# own. Unset entirely (or per-repo) falls back to the interactive agent
# session.
repos: []
